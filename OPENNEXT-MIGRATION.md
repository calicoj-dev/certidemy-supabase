# OPENNEXT-MIGRATION.md

**Status: AGREED, NOT STARTED.** Nothing in this plan has been executed.

**Why this file exists.** `HANDOFF-v9_0.md` §5 was the only written record of
this work, and §7 items 1–2 pointed at work with no document behind them. The
plan was agreed across a chat that ends. **It has to live on disk**, because the
reasoning below is the part that would otherwise be re-derived, and re-derived
wrongly — several of the conclusions here are the opposite of what a careful
person would assume.

The measurements were taken in `../certidemy-web`. This document is the agreed
plan, the evidence for it, and the reasoning.

**Read `HANDOFF-v9_0.md` first** for why the migration is happening at all. This
document does not restate it.

---

## 1. The open question, answered: it is a migration

`HANDOFF-v9_0.md` §7 item 1 said the `_headers` question *"decides whether §5 is
a migration or a redesign."*

**It is a migration.**

### `public/_headers` is honoured, and the adapter's own instructions rely on it

`@opennextjs/cloudflare` supports it, and not incidentally: **it is in the
adapter's own get-started instructions.** Cloudflare Static Assets default to
`max-age=0, must-revalidate`, and matching Next's immutable-asset behaviour for
`/_next/static/*` is done **through that file**. An adapter whose setup
documentation tells you to write a `_headers` file is not one that ignores it.

### The real constraint is narrower than "does it work"

**Headers declared in `_headers` are NOT applied to responses generated by
Worker code.** They apply to static assets.

So the question is not *"is the file read"* — it is *"is every rule in it
pointed at a static asset, or is one of them pointed at a route?"* A rule
matching a Worker-generated path would go silent, and silently: the response
still returns, without the header.

### All five rules are class (a), and that was resolved by listing

Every rule matches **real files under `public/`**, established by listing the
directory rather than by reasoning about the paths:

- 9 brand PNGs
- 12 badge PNGs
- 4 og PNGs
- `apple-touch-icon.png`
- the Next build output (`/_next/static/*`)

**No route under `app/` emits any matched path.** There is no `ImageResponse`
and no `next/og` anywhere in the repository — so there is no generated image
route that could collide with the image paths these rules cover.

**Nothing goes silent.** Conditional on `assets.run_worker_first: false`, which
step 1.4 sets.

### The near-miss, recorded because the next reader will trip on it

**`app/credentials/[code]/badge/route.ts` has "badge" in its name.** It serves
`/credentials/<CODE>/badge` — a Worker-generated response.

**It is matched by no rule.** The badge rule covers `/badges/*` under `public/`;
this route is under `/credentials/`. The two are unrelated and the collision is
in the word, not the path.

Written down because "badge" appearing in both a `_headers` rule and a route
filename is exactly the coincidence someone re-checking this will find, and it
looks like the counter-example that breaks the conclusion. It is not.

---

## 2. Corrections to `HANDOFF-v9_0.md`

Stated as corrections so the two documents can be read together.

### 81 files, 81 pins — not 84/84

v9.0 §5 says *"84 edge pins to remove, across 84 files"*, and even shows its
working. **It is 81, and the working was done on the wrong side of `.gitignore`.**

```
tracked source, exact statement          81 files, 81 pins
loose substring, all files               84 files
```

**The over-count is five files**, of which v9.0's scoped grep saw three:

- `CLAUDE.md` — prose
- two scripts carrying the pin as a **string literal**
- **two `.bak` files** — `app/[locale]/(app)/dashboard/page.tsx.bak` and
  `app/[locale]/verify/[id]/page.tsx.bak`

**The `.bak` files are the interesting half.** They contain *real export
statements* — not prose, not literals — which Next never compiles. They are
untracked, and `*.bak` is `.gitignore:64`.

**That is almost certainly where 84 came from**: a grep of the working tree
counts files git does not have. A measurement taken on the wrong side of the
ignore rule.

### Two prose mentions a loose grep cannot see as prose

- `app/[locale]/layout.tsx:40` — *"child routes (under each route group) declare
  `runtime = "edge"` without…"*
- `app/[locale]/not-found.tsx:14` — *"NO `runtime = "edge"` HERE. not-found.tsx
  is a convention file, not a route…"*

**A removal script anchored on the loose substring would delete the second
one** — the sentence explaining why that file deliberately has no pin. The
comment would go, the absence would look accidental, and the next person would
add a pin to a file that must not have one.

This is why step 1.3 anchors on the exact statement and asserts both directions.

### ZERO adapter API surface — the largest de-risking fact, and v9.0 does not have it

- **`getRequestContext` call sites: 0**
- **`@cloudflare/next-on-pages` imports in source: 0**

The only two references to the adapter in `app/`, `lib/` and `components/` are
**comments** — `app/robots.ts:7` and `components/brand/brand-loader.tsx:10`.

**So the migration touches build configuration and 81 one-line exports. It does
not touch application logic at all.** Nothing calls into the adapter, so nothing
has to be rewritten against a different API.

**This is the single most important fact for sizing the work**, and v9.0 —
which frames the migration as a large piece — does not contain it. The scope is
wide and shallow, not deep.

### The peer dependencies, measured

- **`rclone.js` ^0.6.6 is an OPTIONAL peer** — `peerDependenciesMeta` marks it
  `optional: true`.
- **`next` and `wrangler` are hard peers**, with no optional marking, and
  `npm ci` enforces both.

---

## 3. The sequencing constraint: the split is impossible

The obvious plan — *upgrade Next first, separately, so the attribution is not
lost* — **is not available at any price.**

### The peer ranges do not intersect

```
next-on-pages          next >=14.3.0 <=15.5.2
@opennextjs/cloudflare  next >=15.5.24 <16 || >=16.3.3
intersects              false
```

**The gap is exactly 15.5.3 through 15.5.23** — versions neither adapter
accepts. There is no Next version that satisfies both, so there is no
intermediate state in which one change has landed and the other has not.

**Any Next ≥ 15.5.24 fails `npm` clean-install while `next-on-pages` is still a
dependency** — the identical `ERESOLVE` that already failed CI.

**So the adapter swap and the Next upgrade land in ONE commit.** See §6 for what
that costs and how it is paid.

### 15.5.24 is not a conservative pick among candidates

It is the only pick.

**It is simultaneously the floor of OpenNext's range and the head of the
published 15 line** — npm's `backport` dist-tag points at it. There is no
"slightly newer 15" to prefer and no "slightly older 15" that OpenNext accepts.

**Pin exact. No caret.**

### Next 16.3.3 is forbidden

OpenNext's peer range has a second branch, `>=16.3.3`. **Do not take it.**

`next-intl@3.26.5` peers `next: ^15.0.0`. Next 16 is outside it. The Next 16
door stays closed until `next-intl` is upgraded, **which is its own change with
its own i18n review surface across three locales.**

---

## 4. The plan

**Four phases and a rollback.** (An earlier brief said five phases; the plan as
agreed is Phase 0 through Phase 3, plus the Rollback section. There is no
missing phase.)

**How to read the markings:**

- **R / NR** — reversible, or not. **There is exactly one NR step in this plan**
  and it is 3.3.
- **Validation tier** — where the step can be checked. `LOCALLY VALIDATED` means
  a command on your machine settles it. `CI ONLY` means it is settled by a
  build. `PRODUCTION ONLY` means nothing short of the live system answers it.

---

### Phase 0 — before any code

| # | Step | R/NR | Validation |
|---|---|---|---|
| 0.1 | Record the current Pages project's build command, output dir, Node version and full environment-variable set from the dashboard. `wrangler.jsonc` shows four `NEXT_PUBLIC_*` vars; whether the dashboard sets more is not measured — it cannot be read from the repo. | R | PRODUCTION ONLY (dashboard read) |
| 0.2 | Establish the baseline: `npm run deploy:check` against `https://certidemy.com`. **It will fail — that is the point.** It is the before-figure. | R | PRODUCTION ONLY |

---

### Phase 1 — the atomic change

See **§5 #1**: this cannot be split.

| # | Step | R/NR | Validation |
|---|---|---|---|
| 1.1 | Branch `opennext-migration`. | R | — |
| 1.2 | Remove `@cloudflare/next-on-pages`; add `@opennextjs/cloudflare@1.20.4`; `next` and `eslint-config-next` → **15.5.24 exact**; `wrangler` → `^4.125.0`. One commit. | R (branch) | LOCALLY VALIDATED — `npm ci` must resolve; this is the check that caught the last failure |
| 1.3 | Strip the 81 pins. **Anchor on the exact statement `export const runtime = "edge";`, never the loose substring** — see §2. Post-condition asserts both directions. | R | LOCALLY VALIDATED (`npm run build`, `npm run typecheck`) |
| 1.4 | Add `open-next.config.ts`; rewrite `wrangler.jsonc` — drop `pages_build_output_dir`, add `main`, `assets` with **`run_worker_first: false`**, raise `compatibility_date`, keep `nodejs_compat` and the four vars. | R | LOCALLY VALIDATED |
| 1.5 | Replace the three `pages:*` scripts with `opennextjs-cloudflare build / preview / deploy`. **Keep `deploy:check` untouched.** | R | LOCALLY VALIDATED |
| 1.6 | `opennextjs-cloudflare build`, then list the output root to confirm `_headers`, `og/`, `badges/`, `brand/` and `apple-touch-icon.png` are all present as assets. | R | LOCALLY VALIDATED |
| 1.7 | `opennextjs-cloudflare preview` (local Workers runtime). **Load `/en`.** | R | LOCALLY VALIDATED |

**Step 1.2, amended.** `rclone.js` is an optional peer and will not block
resolution; `next` and `wrangler` are hard peers and `npm ci` enforces both.
**`npm ci`, never `npm install`, is the only local command that resolves the way
Cloudflare's build does.** That distinction is what cost a deploy already.

**Step 1.3, amended — the post-condition is a BYTE-LEVEL PROPERTY ASSERTION.**
The original wording was a tally ("81 files changed"). A tally is not enough
here, and the reason is specific:

> The guard runs **after** the edit, and the edit is **emitted text**. That is
> exactly how a NUL byte got into `check-deploy.mjs`. **A NUL makes a file
> invisible to `grep -n` while `grep -c` still counts it** — so a count-based
> guard is blind in precisely the way that produced the problem it is meant to
> catch.

So: read each of the 81 files **as a Buffer** and assert

- **zero bytes in `{<9, 11, 12, 14–31}`**, with a **planted-NUL control** —
  the check must be shown capable of failing
- **zero occurrences** of the pin statement
- **and the negative half**: the **seven convention files still unpinned**, and
  the **two prose mentions unchanged**

Not a tally, and not `grep -a`.

**Step 1.6 is the one Deliverable-1 assertion resting on documentation rather
than a listing.** Everything else in §1 was settled by listing files. This one
is settled by the adapter's docs until this step runs. **Settle it here, before
deploying.**

**Step 1.7 is the first instrument capable of seeing whether the chunk bug
survived.** `next dev` never reproduced it, because the broken module graph
existed only in adapter output — so this is the earliest possible observation.

> **IF `/en` STILL FAILS UNDER PREVIEW, THE PLAN STOPS HERE.** Do not proceed to
> Phase 2 and do not go near DNS. A failure at 1.7 means the premise in §5 #8 is
> wrong, and everything after it was scoped on that premise.

---

### Phase 2 — the workers.dev gate, before DNS

| # | Step | R/NR | Validation |
|---|---|---|---|
| 2.1 | Create a **new Workers project**. Do not touch the Pages project. Set every variable from 0.1 — **including at build time, not only as runtime vars** (§5 #5). | R | PRODUCTION ONLY |
| 2.2 | Deploy the branch to workers.dev. | R | CI ONLY |
| 2.3 | `npm run deploy:check https://<worker>.<subdomain>.workers.dev`. **This is the gate. DNS does not move until it prints ALL PASS.** | R | CI/PRODUCTION |
| 2.4 | Manual on workers.dev: `/lti/jwks` (well-formed keyset), `/lti/select` (zero `<script>`), one console route, one `/verify/<id>`, and `curl -I` on `/_next/static/…` asserting `max-age=31536000, immutable` — **the live proof of the §1 conclusion**. | R | PRODUCTION ONLY |
| 2.5 | Merge to `main` only after 2.3 passes. | R | — |

**Step 2.3, amended.** The original described three assertions — status,
headline, control. **`check-deploy.mjs` now carries five assertions plus a
carrier route**, committed at `fdf8180`. The gate is that script printing
`ALL PASS`, whatever its current assertion count; do not re-describe its
contents here, because that description has already gone stale once.

---

### Phase 3 — cutover

| # | Step | R/NR | Validation |
|---|---|---|---|
| 3.1 | Add `certidemy.com` + `www` as a custom domain on the Worker; remove from the Pages project. | R (see rollback) | PRODUCTION ONLY |
| 3.2 | `npm run deploy:check https://certidemy.com`. | R | PRODUCTION ONLY |
| 3.3 | Leave the Pages project deployed and untouched for a defined soak. **Deleting it is the only NR step in this plan — do it last, or never.** | NR | — |

**Step 3.1 — time it while you are doing it.** Attaching the custom domain to
the Worker **is the rollback operation in the other direction**, and it is the
only cheap opportunity to measure how long that takes before you need the number
under pressure. See the Rollback section.

**Step 3.3 — the soak is 72 hours**, with `check-deploy.mjs` run at the start
and at the end. Two runs, not one: a single pass at the beginning proves the
cutover, and only the second proves it held.

---

### Rollback

**What must be true:** the Pages project still exists with its last successful
deployment (the `4a175a0` build) intact, and its build settings and environment
variables unchanged. **Nothing in Phases 1–2 touches it; Phase 3.1 only
detaches the domain.**

**The action:** remove the custom domain from the Worker, re-add it to the Pages
project. **No rebuild, no revert commit, no redeploy** — the artifact is already
sitting there.

**How long it takes: NOT MEASURED.** Custom-domain reassignment is
Cloudflare-internal and normally propagates in minutes, but it has not been
timed on this account and **no number is put on it here.** Time it during
Phase 3.1.

**Rollback restores the site to today's behaviour, which includes the homepage
500.** That is the point: **it is a rollback, not a fix.**

---

## 5. What could make this plan wrong

### #1 — The two adapters' peer ranges do not intersect, so "upgrade Next first, separately" is impossible

```
next-on-pages : >=14.3.0 <=15.5.2
opennext      : >=15.5.24 <16 || >=16.3.3
intersects    : false
```

There is no Next version that satisfies both. The gap is exactly
15.5.3 … 15.5.23 — versions neither adapter accepts.

This kills the sequencing decided earlier in this work (*"upgrade Next first and
separately so attribution isn't lost"*). Any Next ≥ 15.5.24 fails npm
clean-install while `next-on-pages` is a dependency — the identical `ERESOLVE`
that failed CI on 15.5.24 already. The Next upgrade and the adapter swap must
land in one commit, and **the attribution that separation was meant to preserve
is not available at any price.** Plan the commit message to carry what the split
would have carried.

### #2 — The jump is 15.1.4 → 15.5.24, and 15.5.2 in between is a known unexplained CI failure with no local reproduction

The last version proven to deploy here is 15.1.4. The lowest OpenNext accepts is
15.5.24. Every version between them is either unproven or actively failed.

The mitigating fact, and it is a strong one: the 15.5.2 CI refusal was
`next-on-pages`' own edge-runtime check (`/_not-found` not configured to run
with the Edge Runtime). OpenNext forbids the edge runtime entirely — there is no
such check to fail. So the specific failure is expected to evaporate rather than
be fixed.

But that is a prediction, not a measurement, and it is the plan's central bet.
If 15.5.24 fails for some other reason, you find out at step 2.2 with both
changes entangled and no way to bisect them. Phase 2 is structured so that
discovery happens on workers.dev with production untouched — that is the whole
reason the gate sits before DNS.

> **[CORRECTION — the mitigation above is DEMOTED, and must not be
> re-strengthened.]**
>
> The paragraph calling it *"a strong one"* rests on an explanation that has
> since been **retracted**. `HANDOFF-v9_0.md` §3 records it: the claim that
> *"Next 15.5.2 emits `/_not-found` as `nodejs22.x` regardless of the edge pin"*
> **does not reproduce.** Four full builds on 15.5.2 — cached, clean, Node 24,
> and Node 22.16.0 matching CI exactly — **all emit it as edge**, and route by
> route 15.1.4 and 15.5.2 differ in exactly one place: a dedup symlink.
>
> **The inference survives only as a CONDITIONAL.** *If* the refusal was
> `next-on-pages`' edge-runtime check, then OpenNext has no equivalent and it
> evaporates. **What is gone is confidence in the antecedent, not the
> implication.**
>
> **And the baseline undercuts it from a second, independent direction.**
> `next-on-pages` ships **two `nodejs22.x` functions on 15.1.4 in a build that
> deploys** — `_error.func` and `favicon.ico.rsc.func`. **Its error message
> states a rule it does not enforce.** So *"a Node function appeared where an
> edge one was required"* is not, by itself, an explanation for anything.
>
> **The consequence, which is what a reader at the gate actually needs:**
>
> **THE BET IS UNCHANGED IN SIZE, AND THE PLAN IS ALREADY STRUCTURED FOR IT.**
> Step 2.3 exists precisely because a 15.5.24 failure *for some other reason*
> would surface with three majors entangled and no way to bisect — on
> workers.dev, with production untouched.
>
> **Demoting the mitigation changes no step. It changes what someone believes
> when they read the failure.** Which matters: a reader who thinks the mechanism
> is understood will look for a variant of it, and the trigger is unknown.

### #3 — wrangler must go 3.114.17 → v4, a second unvalidated major in the same commit

OpenNext peers `wrangler: ^4.125.0`; installed is 3.114.17. Unavoidable, but
name it — it is a third moving part in an already-atomic change, and **a wrangler
v4 config-parsing difference would present as an OpenNext failure.**

### #4 — Do not take the `>=16.3.3` branch of OpenNext's peer range

`next-intl@3.26.5` peers `next: ^15.0.0`. Next 16 is outside it. Pin 15.5.24,
and treat the Next 16 door as closed until `next-intl` is upgraded — which is
its own change with its own i18n review surface across three locales.

### #5 — `NEXT_PUBLIC_*` are baked at build time, and `wrangler.jsonc` vars are runtime-only

All four are `NEXT_PUBLIC_`, so Next inlines them into the client bundle during
`next build`. **The `vars` block satisfies the Worker at runtime; it does not
satisfy the build.** Today the Pages dashboard supplies them to the build
environment.

If the new Workers project's build environment is configured from
`wrangler.jsonc` alone, the client bundle ships with `undefined` for the
Supabase URL and anon key — and `NEXT_PUBLIC_EDGE_FUNCTIONS_URL` is the variable
this repo already has four call sites for **with no fallback**, one of which
(`issuing-panel.tsx`, `?? ""`) yields a **relative URL rather than failing**.
**That is a silent-wrong-host failure, not a crash.**

> **[SUPERSEDED — the final paragraph only.]** The original continued: *"`npm run
> deploy:check` would not catch it — the homepage renders fine without Supabase.
> Add an explicit assertion at step 2.4."*
>
> **That gap is closed.** `check-deploy.mjs` assertions three and four assert
> the **Supabase ref and the anon key in the served bytes**, and
> `/en/certifications` is checked every run as the observed passing case. **The
> manual 2.4 addition it proposed is withdrawn** — the gate at 2.3 now covers it.
>
> The rest of #5 stands unchanged, and the risk it names is real. Only the
> remedy moved.

### #6 — `app/robots.ts`'s docblock becomes false, and acting on it breaks §1

Its comment at line 7 describes `@cloudflare/next-on-pages` failing the build
for any route without an edge pin. Under OpenNext that is not true.

**Correct the comment; do not move the icons back into `app/`.** The icons live
under `public/` because that is what makes every `_headers` rule class (a), and
a reader following the stale docblock would undo §1's conclusion while believing
they were fixing an inconsistency.

### #7 — ANSWERED: `credentials.certidemy.com` is unaffected

It is **a separate Worker in its own repo**, deliberately not git-connected, and
deployed by hand. **Phase 3.1 detaches the apex and nothing else.**

The four Open Badges identifier URLs it serves are immovable — a credential in
the wild resolves them — which is why this was checked before any DNS step was
written. It is not at risk.

### #8 — The plan does NOT claim the migration fixes the 500

**This is the one to keep prominent.**

OpenNext does not use the emitter that produced the bad chunk, so **the defect
has no mechanism to survive.** That is the expectation the whole plan is built
on.

**But it is reasoning, not measurement.** Step 1.7 is the first thing that can
settle it, and the plan stops there if it does not.

Nothing in Phases 2–3 is justified by "the 500 will be fixed". They are
justified by the adapter being archived, defective and unupgradable — which is
true regardless of what 1.7 shows.

---

## 6. The commit message must carry what the split cannot

**Because §3 makes the split impossible, the attribution it would have preserved
has nowhere else to live.**

Three changes land in one commit: **the adapter swap**, **the Next upgrade
15.1.4 → 15.5.24**, and **the wrangler major 3 → 4**. A bisect cannot separate
them, because there is no revision in which one landed without the others — the
peer ranges forbid it.

**So the Phase 1 commit message must state which change is which**: which lines
are the adapter, which are Next, which are wrangler, and what each was expected
to do.

**A future bisect will land on that commit and have to read prose, because it
cannot have commits.** Write the message for that reader.

---

## 7. What this cost to learn

Only the durable ones. Each was paid for.

### Local build output does not predict the deployed artifact

**Production chunks differently from local.** The four production carriers of
the Supabase credentials **are not in the local manifest's carrier list at
all.**

So a check anchored on a chunk filename — the obvious way to write it — **would
have reported a healthy build as a missing environment.** The anchor has to be
something the deploy cannot rename.

### A needle must be derived from the source of truth, never hand-copied

**A hand-truncated JWT prefix produced a FALSE ZERO on a healthy build.** The
needle was wrong, the code was right, and **the output was silent about which**
— a zero result looks identical whether nothing is there or you asked for the
wrong thing.

Derive the needle from the same file the code reads. `check-deploy.mjs` reads
its headline from `messages/<locale>.json` for exactly this reason.

### A control proves an assertion can FAIL. It does not prove it can PASS.

**The 500 shell and the 404 shell load byte-identical script sets.** So on a
broken origin, the control could not discriminate two of the five assertions —
both the real page and the bogus path produced the same evidence.

**Only a known-good carrier route closed that.** A control establishes the
negative half; a route known to carry the thing establishes the positive half.
**Both halves, or the check is only half instrumented.**

### An error or 404 shell loads only framework chunks — and this is the structural one

In any build, under any chunking, an error or 404 shell loads framework chunks
and nothing else.

**That is structural and survives a chunking change.** The manifest-derived
justification for the same conclusion did not — it was true of one build's
output and would have gone stale the moment the bundler moved.

**When two arguments reach the same place, keep the one that does not depend on
today's artifact.**
