# The bilingual queue — 58 paragraphs, 35 rows

Paragraphs inside review-gated rows whose translation predates the English repair. Every other gated paragraph was re-translated FROM repaired English and cannot carry ISO's sentence, because the source no longer has it.

**The limit on this list:** This cannot confirm a leak. The index is English-only, so a translated row measures zero by construction (IP-POSITION section 6). It reports PROVENANCE, which is exact, not leak status, which is unknowable here.

**The question for each one:** does the translation render the REPAIRED English,or the ISO sentence it replaced? If the former, it needs only a register check.If the latter, it is a reproduction of ISO's expression in a language noinstrument here can measure, and it needs rewriting.

---

## AIMS-IA

### aims-ia-02-07-the-programme-audits-itself · es-419  (1 paragraph)

**1.** block 2, line 6

English now:

```
ISO/IEC 42001 clause 9.2.2 makes the connection to the next cycle a requirement: when establishing the audit programme, the organization **shall weigh how important the audited processes are and what earlier audits found**. A review that produces no change to the following programme has not been used.
```

Translation now:

```
El apartado 9.2.2 de ISO/IEC 42001 convierte la conexión con el siguiente ciclo en un requisito: al establecer el programa de auditoría, la organización **deberá considerar la importancia de los procesos involucrados y los resultados de auditorías anteriores**. Una revisión que no produce ningún cambio en el programa siguiente no ha sido utilizada.
```

- [ ] renders the repaired English — register ok
- [ ] renders the repaired English — register needs work: 
- [ ] still renders ISO's sentence — rewrite

### aims-ia-03-03-what-the-sample-supports · es-419  (1 paragraph)

**2.** block 1, line 0

English now:

```
ISO 19011:2026 Annex A.6.1 defines audit sampling plainly: it is used where examining all the available information is impractical or too costly - records too numerous, or too dispersed, to justify examining every item. It is the process of selecting **less than one hundred per cent** of the items in a population, so that evidence about some characteristic of it can be gathered and weighed, and a conclusion about the whole drawn.
```

Translation now:

```
El Anexo A.6.1 de ISO 19011:2026 define el muestreo de auditoría con claridad: se aplica cuando no es práctico ni rentable examinar toda la información disponible — registros demasiado numerosos o dispersos para justificar el examen de cada elemento. Es el proceso de seleccionar **menos del cien por ciento** de los elementos de una población para obtener y evaluar evidencia sobre alguna característica de esa población, con el fin de formular una conclusión sobre ella.
```

- [ ] renders the repaired English — register ok
- [ ] renders the repaired English — register needs work: 
- [ ] still renders ISO's sentence — rewrite

### aims-ia-03-03-what-the-sample-supports · pt-BR  (1 paragraph)

**3.** block 1, line 0

English now:

```
ISO 19011:2026 Annex A.6.1 defines audit sampling plainly: it is used where examining all the available information is impractical or too costly - records too numerous, or too dispersed, to justify examining every item. It is the process of selecting **less than one hundred per cent** of the items in a population, so that evidence about some characteristic of it can be gathered and weighed, and a conclusion about the whole drawn.
```

Translation now:

```
O Anexo A.6.1 da ISO 19011:2026 define amostragem em auditoria de forma direta: ela ocorre quando não é prático ou economicamente viável examinar todas as informações disponíveis — registros numerosos demais ou dispersos demais para justificar o exame de cada item. É o processo de selecionar **menos de cem por cento** dos itens de uma população para obter e avaliar evidências sobre alguma característica dessa população, a fim de formar uma conclusão a seu respeito.
```

- [ ] renders the repaired English — register ok
- [ ] renders the repaired English — register needs work: 
- [ ] still renders ISO's sentence — rewrite

### aims-ia-03-04-demonstrated-not-described · pt-BR  (1 paragraph)

**4.** block 5, line 0

English now:

```
Clause 4.7 describes the evidence-based approach as the rational way to reach **reliable and reproducible** audit conclusions within a systematic process.
```

Translation now:

```
A Seção 4.7 descreve a abordagem baseada em evidências como o método racional para alcançar conclusões de auditoria **confiáveis e reproduzíveis** em um processo de auditoria sistemático.
```

- [ ] renders the repaired English — register ok
- [ ] renders the repaired English — register needs work: 
- [ ] still renders ISO's sentence — rewrite

### aims-ia-03-06-what-a-screen-share-establishes · es-419  (1 paragraph)

**5.** block 1, line 1

English now:

```
ISO 19011:2026 Annex A.16 describes an audit using remote methods as one that **uses technology during the audit process** - for observing, interviewing and verifying objective evidence. It records that remote methods can bring benefits for auditor and auditee, that they can be **necessary for the audit of virtual locations**, and that they can bring **additional risks and opportunities** into the audit process.
```

Translation now:

```
El Anexo A.16 de ISO 19011:2026 describe una auditoría que utiliza métodos remotos como aquella que **usa tecnología durante el proceso de auditoría** — para observar, entrevistar y verificar evidencia objetiva. Señala que los métodos remotos pueden aportar beneficios tanto al auditor como al auditado, que pueden ser **necesarios para la auditoría de ubicaciones virtuales**, y que pueden introducir **riesgos y oportunidades adicionales** en el proceso de auditoría.
```

- [ ] renders the repaired English — register ok
- [ ] renders the repaired English — register needs work: 
- [ ] still renders ISO's sentence — rewrite

### aims-ia-03-06-what-a-screen-share-establishes · pt-BR  (1 paragraph)

**6.** block 1, line 1

English now:

```
ISO 19011:2026 Annex A.16 describes an audit using remote methods as one that **uses technology during the audit process** - for observing, interviewing and verifying objective evidence. It records that remote methods can bring benefits for auditor and auditee, that they can be **necessary for the audit of virtual locations**, and that they can bring **additional risks and opportunities** into the audit process.
```

Translation now:

```
O Anexo A.16 da ISO 19011:2026 descreve uma auditoria que utiliza métodos remotos como aquela que **usa tecnologia durante o processo de auditoria** — para observar, entrevistar e verificar evidências objetivas. Registra que os métodos remotos podem trazer benefícios para o auditor e para o auditado, que podem ser **necessários para a auditoria de locais virtuais** e que podem introduzir **riscos e oportunidades adicionais** ao processo de auditoria.
```

- [ ] renders the repaired English — register ok
- [ ] renders the repaired English — register needs work: 
- [ ] still renders ISO's sentence — rewrite

### aims-ia-04-03-leadership-in-artifacts · es-419  (2 paragraphs)

**7.** block 2, line 2

English now:

```
- a) **suits the organization's purpose**;
```

Translation now:

```
- a) sea **apropiada al propósito** de la organización;
```

- [ ] renders the repaired English — register ok
- [ ] renders the repaired English — register needs work: 
- [ ] still renders ISO's sentence — rewrite

**8.** block 2, line 8

English now:

```
- **point to other organizational policies where relevant**;
```

Translation now:

```
- **hacer referencia, según corresponda, a otras políticas organizacionales**;
```

- [ ] renders the repaired English — register ok
- [ ] renders the repaired English — register needs work: 
- [ ] still renders ISO's sentence — rewrite

### aims-ia-04-03-leadership-in-artifacts · pt-BR  (3 paragraphs)

**9.** block 1, line 0

English now:

```
ISO/IEC 42001:2023 clause 5.1 requires top management to show leadership and commitment for the AI management system by:
```

Translation now:

```
A Seção 5.1 da ISO/IEC 42001:2023 exige que a alta direção demonstre liderança e comprometimento em relação ao sistema de gestão de IA:
```

- [ ] renders the repaired English — register ok
- [ ] renders the repaired English — register needs work: 
- [ ] still renders ISO's sentence — rewrite

**10.** block 2, line 8

English now:

```
- **point to other organizational policies where relevant**;
```

Translation now:

```
- **fazer referência, conforme pertinente, a outras políticas organizacionais**;
```

- [ ] renders the repaired English — register ok
- [ ] renders the repaired English — register needs work: 
- [ ] still renders ISO's sentence — rewrite

**11.** block 2, line 10

English now:

```
- be **open to interested parties where appropriate**.
```

Translation now:

```
- estar **disponível para as partes interessadas, conforme apropriado**.
```

- [ ] renders the repaired English — register ok
- [ ] renders the repaired English — register needs work: 
- [ ] still renders ISO's sentence — rewrite

### aims-ia-04-04-criteria-before-assessment · es-419  (1 paragraph)

**12.** block 6, line 0

English now:

```
Clause 6.1.2 d) 1) asks the analysis to weigh what might follow **for the organization, for individuals and for societies** if an identified risk came about.
```

Translation now:

```
El apartado 6.1.2 d) 1) pide que el análisis evalúe las posibles consecuencias **para la organización, las personas y las sociedades** que se derivarían si los riesgos identificados se materializaran.
```

- [ ] renders the repaired English — register ok
- [ ] renders the repaired English — register needs work: 
- [ ] still renders ISO's sentence — rewrite

### aims-ia-04-04-criteria-before-assessment · pt-BR  (1 paragraph)

**13.** block 6, line 0

English now:

```
Clause 6.1.2 d) 1) asks the analysis to weigh what might follow **for the organization, for individuals and for societies** if an identified risk came about.
```

Translation now:

```
A Seção 6.1.2 d) 1) pede que a análise avalie as potenciais consequências **para a organização, para indivíduos e para a sociedade** que resultariam caso os riscos identificados se materializassem.
```

- [ ] renders the repaired English — register ok
- [ ] renders the repaired English — register needs work: 
- [ ] still renders ISO's sentence — rewrite

### aims-ia-04-05-controls-first-annex-a-second · pt-BR  (1 paragraph)

**14.** block 1, line 0

English now:

```
ISO/IEC 42001:2023 clause 6.1.3 requires the organization to set out a process for treating AI risk that will:
```

Translation now:

```
A Seção 6.1.3 da norma ISO/IEC 42001:2023 exige que a organização defina um processo de tratamento de riscos de IA para:
```

- [ ] renders the repaired English — register ok
- [ ] renders the repaired English — register needs work: 
- [ ] still renders ISO's sentence — rewrite

### aims-ia-04-06-two-assessments-not-one · es-419  (1 paragraph)

**15.** block 1, line 4

English now:

```
- The assessment's **result shall be documented**.
```

Translation now:

```
- El **resultado** de la evaluación de impacto del sistema de IA **deberá estar documentado**.
```

- [ ] renders the repaired English — register ok
- [ ] renders the repaired English — register needs work: 
- [ ] still renders ISO's sentence — rewrite

### aims-ia-04-07-controlled-not-merely-present · pt-BR  (1 paragraph)

**16.** block 1, line 0

English now:

```
ISO/IEC 42001:2023 clause 7.5.1 requires the AI management system to hold:
```

Translation now:

```
A Seção 7.5.1 da ISO/IEC 42001:2023 exige que o sistema de gestão de IA da organização inclua:
```

- [ ] renders the repaired English — register ok
- [ ] renders the repaired English — register needs work: 
- [ ] still renders ISO's sentence — rewrite

### aims-ia-04-08-defined-versus-running · pt-BR  (1 paragraph)

**17.** block 1, line 1

English now:

```
- **setting criteria for those processes**;
```

Translation now:

```
- **estabelecimento de critérios para os processos**;
```

- [ ] renders the repaired English — register ok
- [ ] renders the repaired English — register needs work: 
- [ ] still renders ISO's sentence — rewrite

### aims-ia-04-09-normative-and-should · es-419  (2 paragraphs)

**18.** block 1, line 6

English now:

```
| D | Using the AI management system across domains or sectors | informative | `should`, `can` |
```

Translation now:

```
| D | Uso del sistema de gestión de IA en dominios o sectores | informativo | `should`, `can` |
```

- [ ] renders the repaired English — register ok
- [ ] renders the repaired English — register needs work: 
- [ ] still renders ISO's sentence — rewrite

**19.** block 3, line 6

English now:

```
- **is not always suitable or sufficient in every situation** and does not always meet an organization's specific control requirements;
```

Translation now:

```
- **no siempre es adecuada ni suficiente en todas las situaciones** y no siempre satisface los requisitos de control específicos de una organización;
```

- [ ] renders the repaired English — register ok
- [ ] renders the repaired English — register needs work: 
- [ ] still renders ISO's sentence — rewrite

### aims-ia-04-09-normative-and-should · pt-BR  (3 paragraphs)

**20.** block 1, line 6

English now:

```
| D | Using the AI management system across domains or sectors | informative | `should`, `can` |
```

Translation now:

```
| D | Uso do sistema de gestão de IA em domínios ou setores | informativo | `should`, `can` |
```

- [ ] renders the repaired English — register ok
- [ ] renders the repaired English — register needs work: 
- [ ] still renders ISO's sentence — rewrite

**21.** block 3, line 5

English now:

```
- **but no organization has to document or justify including or excluding implementation guidance in its statement of applicability** (see 6.1.3);
```

Translation now:

```
- **mas as organizações não precisam documentar ou justificar a inclusão ou exclusão de orientações de implementação na declaração de aplicabilidade** (ver 6.1.3);
```

- [ ] renders the repaired English — register ok
- [ ] renders the repaired English — register needs work: 
- [ ] still renders ISO's sentence — rewrite

**22.** block 3, line 6

English now:

```
- **is not always suitable or sufficient in every situation** and does not always meet an organization's specific control requirements;
```

Translation now:

```
- **nem sempre é adequada ou suficiente em todas as situações** e nem sempre atende aos requisitos específicos de controle de uma organização;
```

- [ ] renders the repaired English — register ok
- [ ] renders the repaired English — register needs work: 
- [ ] still renders ISO's sentence — rewrite

### aims-ia-04-10-justifying-both-directions · es-419  (2 paragraphs)

**23.** block 1, line 0

English now:

```
ISO/IEC 42001:2023 clause 6.1.3 f) requires the organization to produce a statement of applicability **holding the necessary controls** and **justifying both inclusions and exclusions**.
```

Translation now:

```
El apartado 6.1.3 f) de la norma ISO/IEC 42001:2023 requiere que la organización elabore una Declaración de Aplicabilidad que **contenga los controles necesarios** y **proporcione justificación para las inclusiones y exclusiones**.
```

- [ ] renders the repaired English — register ok
- [ ] renders the repaired English — register needs work: 
- [ ] still renders ISO's sentence — rewrite

**24.** block 2, line 5

English now:

```
- **Right:** *Where A.2.2 is selected, the organization shall document a policy covering how AI systems are developed or used.*
```

Translation now:

```
- **Correcto:** *Cuando se selecciona A.2.2, la organización deberá documentar una política para el desarrollo o uso de sistemas de IA.*
```

- [ ] renders the repaired English — register ok
- [ ] renders the repaired English — register needs work: 
- [ ] still renders ISO's sentence — rewrite

### aims-ia-04-10-justifying-both-directions · pt-BR  (1 paragraph)

**25.** block 2, line 1

English now:

```
> The controls set out in Table A.1 give the organization a reference for meeting its objectives and handling risks that arise from designing and operating AI systems. **Not every control objective and control in Table A.1 has to be used, and an organization can design and implement controls of its own (see 6.1.3).**
```

Translation now:

```
> Os controles detalhados na Tabela A.1 fornecem à organização uma referência para atender aos objetivos organizacionais e abordar os riscos relacionados ao projeto e à operação de sistemas de IA. **Nem todos os objetivos de controle e controles listados na Tabela A.1 precisam ser utilizados, e a organização pode projetar e implementar seus próprios controles (ver 6.1.3).**
```

- [ ] renders the repaired English — register ok
- [ ] renders the repaired English — register needs work: 
- [ ] still renders ISO's sentence — rewrite

### aims-ia-04-13-competence-the-organization-claims · pt-BR  (2 paragraphs)

**26.** block 1, line 1

English now:

```
- **determine the necessary competence** of anyone working under its control whose work affects AI performance;
```

Translation now:

```
- **determine a competência necessária** das pessoas que realizam trabalho sob seu controle e que afeta seu desempenho de IA;
```

- [ ] renders the repaired English — register ok
- [ ] renders the repaired English — register needs work: 
- [ ] still renders ISO's sentence — rewrite

**27.** block 1, line 4

English now:

```
- and **appropriate documented information shall be kept as evidence of competence**.
```

Translation now:

```
- e que **informação documentada apropriada esteja disponível como evidência de competência**.
```

- [ ] renders the repaired English — register ok
- [ ] renders the repaired English — register needs work: 
- [ ] still renders ISO's sentence — rewrite

### aims-ia-05-05-a-report-for-someone-not-in-the-room · es-419  (4 paragraphs)

**28.** block 1, line 1

English now:

```
- a) the audit's **objectives**;
```

Translation now:

```
- a) los **objetivos** de la auditoría;
```

- [ ] renders the repaired English — register ok
- [ ] renders the repaired English — register needs work: 
- [ ] still renders ISO's sentence — rewrite

**29.** block 1, line 6

English now:

```
- f) the audit **criteria**;
```

Translation now:

```
- f) los **criterios** de auditoría;
```

- [ ] renders the repaired English — register ok
- [ ] renders the repaired English — register needs work: 
- [ ] still renders ISO's sentence — rewrite

**30.** block 1, line 8

English now:

```
- h) the audit **conclusions**;
```

Translation now:

```
- h) las **conclusiones** de la auditoría;
```

- [ ] renders the repaired English — register ok
- [ ] renders the repaired English — register needs work: 
- [ ] still renders ISO's sentence — rewrite

**31.** block 7, line 0

English now:

```
Clause 6.5.1 h) asks for audit conclusions and i) asks for **a statement of how far the audit criteria have been fulfilled**. They are different things and the second is skipped almost universally.
```

Translation now:

```
El apartado 6.5.1 h) pide las conclusiones de la auditoría y el i) pide **una declaración sobre el grado en que se han cumplido los criterios de auditoría**. Son cosas distintas y la segunda se omite casi universalmente.
```

- [ ] renders the repaired English — register ok
- [ ] renders the repaired English — register needs work: 
- [ ] still renders ISO's sentence — rewrite

### aims-ia-05-05-a-report-for-someone-not-in-the-room · pt-BR  (2 paragraphs)

**32.** block 1, line 3

English now:

```
- c) who the **audit client** is;
```

Translation now:

```
- c) identificação do **cliente da auditoria**;
```

- [ ] renders the repaired English — register ok
- [ ] renders the repaired English — register needs work: 
- [ ] still renders ISO's sentence — rewrite

**33.** block 1, line 9

English now:

```
- i) a statement of **how far the audit criteria have been fulfilled**;
```

Translation now:

```
- i) uma declaração sobre o **grau em que os critérios de auditoria foram atendidos**;
```

- [ ] renders the repaired English — register ok
- [ ] renders the repaired English — register needs work: 
- [ ] still renders ISO's sentence — rewrite

### aims-ia-05-06-correction-is-not-corrective-action · pt-BR  (3 paragraphs)

**34.** block 1, line 0

English now:

```
ISO/IEC 42001:2023 clause 10.2 states that where a nonconformity arises, the organization shall:
```

Translation now:

```
A Seção 10.2 da ISO/IEC 42001:2023 determina que, quando uma não conformidade ocorre, a organização deve:
```

- [ ] renders the repaired English — register ok
- [ ] renders the repaired English — register needs work: 
- [ ] still renders ISO's sentence — rewrite

**35.** block 1, line 4

English now:

```
- d) **review how effective** any corrective action was;
```

Translation now:

```
- d) **analisar a eficácia** de qualquer ação corretiva tomada;
```

- [ ] renders the repaired English — register ok
- [ ] renders the repaired English — register needs work: 
- [ ] still renders ISO's sentence — rewrite

**36.** block 1, line 5

English now:

```
- e) **change the AI management system**, if that is necessary.
```

Translation now:

```
- e) **realizar mudanças no sistema de gestão de IA**, se necessário.
```

- [ ] renders the repaired English — register ok
- [ ] renders the repaired English — register needs work: 
- [ ] still renders ISO's sentence — rewrite

### aims-ia-05-07-where-the-audit-lands · es-419  (2 paragraphs)

**37.** block 2, line 1

English now:

```
- a) where **actions from earlier management reviews** stand;
```

Translation now:

```
- a) el estado de las **acciones de revisiones por la dirección anteriores**;
```

- [ ] renders the repaired English — register ok
- [ ] renders the repaired English — register needs work: 
- [ ] still renders ISO's sentence — rewrite

**38.** block 2, line 4

English now:

```
- d) **information on how the AI management system is performing**, including **trends in**:
```

Translation now:

```
- d) **información sobre el desempeño del sistema de gestión de IA**, incluidas **las tendencias en**:
```

- [ ] renders the repaired English — register ok
- [ ] renders the repaired English — register needs work: 
- [ ] still renders ISO's sentence — rewrite

### aims-ia-05-07-where-the-audit-lands · pt-BR  (1 paragraph)

**39.** block 2, line 4

English now:

```
- d) **information on how the AI management system is performing**, including **trends in**:
```

Translation now:

```
- d) **informações sobre o desempenho do sistema de gestão de IA**, incluindo **tendências em**:
```

- [ ] renders the repaired English — register ok
- [ ] renders the repaired English — register needs work: 
- [ ] still renders ISO's sentence — rewrite

---

## ISMS-IA

### isms-ia-01-02-principles-in-tension · pt-BR  (1 paragraph)

**40.** block 1, line 5

English now:

```
- **Independence** - the ground on which the audit's impartiality and its conclusions' objectivity both rest.
```

Translation now:

```
- **Independência** - a base para a imparcialidade da auditoria e a objetividade de suas conclusões.
```

- [ ] renders the repaired English — register ok
- [ ] renders the repaired English — register needs work: 
- [ ] still renders ISO's sentence — rewrite

### isms-ia-02-04-choosing-the-method · es-419  (1 paragraph)

**41.** block 6, line 0

English now:

```
Annex A.16 is explicit that using remote auditing methods **can bring additional risks and opportunities into the audit process**. These are audit risks in the sense of lesson 02-02 - conditions affecting whether the audit can do its job.
```

Translation now:

```
El Anexo A.16 es explícito en que el uso de métodos de auditoría remota **puede introducir riesgos y oportunidades adicionales en el proceso de auditoría**. Estos son riesgos de auditoría en el sentido de la lección 02-02: condiciones que afectan la capacidad de la auditoría para cumplir su función.
```

- [ ] renders the repaired English — register ok
- [ ] renders the repaired English — register needs work: 
- [ ] still renders ISO's sentence — rewrite

### isms-ia-02-04-choosing-the-method · pt-BR  (1 paragraph)

**42.** block 6, line 0

English now:

```
Annex A.16 is explicit that using remote auditing methods **can bring additional risks and opportunities into the audit process**. These are audit risks in the sense of lesson 02-02 - conditions affecting whether the audit can do its job.
```

Translation now:

```
O Anexo A.16 é explícito ao afirmar que o uso de métodos de auditoria remota **pode introduzir riscos e oportunidades adicionais ao processo de auditoria**. Estes são riscos de auditoria no sentido da lição 02-02 — condições que afetam se a auditoria pode cumprir sua função.
```

- [ ] renders the repaired English — register ok
- [ ] renders the repaired English — register needs work: 
- [ ] still renders ISO's sentence — rewrite

### isms-ia-02-06-testing-the-programme · pt-BR  (1 paragraph)

**43.** block 2, line 14

English now:

```
**And clause 9.2.2's closing requirement:**
```

Translation now:

```
**E o requisito final da cláusula 9.2.2:**
```

- [ ] renders the repaired English — register ok
- [ ] renders the repaired English — register needs work: 
- [ ] still renders ISO's sentence — rewrite

### isms-ia-04-02-demonstrated-not-stated · pt-BR  (2 paragraphs)

**44.** block 1, line 5

English now:

```
- c) ensuring the **resources** needed are in place
```

Translation now:

```
- c) assegurar que os **recursos** necessários estejam disponíveis
```

- [ ] renders the repaired English — register ok
- [ ] renders the repaired English — register needs work: 
- [ ] still renders ISO's sentence — rewrite

**45.** block 4, line 9

English now:

```
- g) be **open to interested parties where appropriate**
```

Translation now:

```
- g) estar **disponível para as partes interessadas, conforme apropriado**
```

- [ ] renders the repaired English — register ok
- [ ] renders the repaired English — register needs work: 
- [ ] still renders ISO's sentence — rewrite

### isms-ia-04-04-tracing-the-soa-back · es-419  (2 paragraphs)

**46.** block 1, line 2

English now:

```
- **why each is included**
```

Translation now:

```
- **la justificación de su inclusión**
```

- [ ] renders the repaired English — register ok
- [ ] renders the repaired English — register needs work: 
- [ ] still renders ISO's sentence — rewrite

**47.** block 1, line 3

English now:

```
- **whether each necessary control is implemented**
```

Translation now:

```
- **si los controles necesarios están implementados o no**
```

- [ ] renders the repaired English — register ok
- [ ] renders the repaired English — register needs work: 
- [ ] still renders ISO's sentence — rewrite

### isms-ia-04-04-tracing-the-soa-back · pt-BR  (1 paragraph)

**48.** block 1, line 3

English now:

```
- **whether each necessary control is implemented**
```

Translation now:

```
- **se os controles necessários estão implementados ou não**
```

- [ ] renders the repaired English — register ok
- [ ] renders the repaired English — register needs work: 
- [ ] still renders ISO's sentence — rewrite

### isms-ia-04-06-defined-versus-running · es-419  (4 paragraphs)

**49.** block 3, line 10

English now:

```
**Two more sentences close clause 9.1:**
```

Translation now:

```
**Dos oraciones más cierran el apartado 9.1:**
```

- [ ] renders the repaired English — register ok
- [ ] renders the repaired English — register needs work: 
- [ ] still renders ISO's sentence — rewrite

**50.** block 3, line 3

English now:

```
- c) **when** monitoring and measuring shall be done
```

Translation now:

```
- c) **cuándo** se realizará el monitoreo y la medición
```

- [ ] renders the repaired English — register ok
- [ ] renders the repaired English — register needs work: 
- [ ] still renders ISO's sentence — rewrite

**51.** block 3, line 5

English now:

```
- e) **when** those results shall be analysed and evaluated
```

Translation now:

```
- e) **cuándo** se analizarán y evaluarán los resultados
```

- [ ] renders the repaired English — register ok
- [ ] renders the repaired English — register needs work: 
- [ ] still renders ISO's sentence — rewrite

**52.** block 6, line 4

English now:

```
- Methods must give comparable and reproducible results if they are to count as valid.
```

Translation now:

```
- Los métodos deben producir resultados comparables y reproducibles para considerarse válidos.
```

- [ ] renders the repaired English — register ok
- [ ] renders the repaired English — register needs work: 
- [ ] still renders ISO's sentence — rewrite

### isms-ia-04-06-defined-versus-running · pt-BR  (4 paragraphs)

**53.** block 3, line 10

English now:

```
**Two more sentences close clause 9.1:**
```

Translation now:

```
**Mais duas frases encerram a seção 9.1:**
```

- [ ] renders the repaired English — register ok
- [ ] renders the repaired English — register needs work: 
- [ ] still renders ISO's sentence — rewrite

**54.** block 2, line 1

English now:

```
- **setting criteria for those processes**
```

Translation now:

```
- **estabelecimento de critérios para os processos**
```

- [ ] renders the repaired English — register ok
- [ ] renders the repaired English — register needs work: 
- [ ] still renders ISO's sentence — rewrite

**55.** block 3, line 14

English now:

```
Note b)'s standard: methods should give **comparable and reproducible results** if they are to count as valid. A metric whose definition changed mid-year is not producing comparable results.
```

Translation now:

```
Observe o padrão de b): convém que os métodos produzam **resultados comparáveis e reproduzíveis** para serem considerados válidos. Uma métrica cuja definição mudou no meio do ano não está produzindo resultados comparáveis.
```

- [ ] renders the repaired English — register ok
- [ ] renders the repaired English — register needs work: 
- [ ] still renders ISO's sentence — rewrite

**56.** block 6, line 4

English now:

```
- Methods must give comparable and reproducible results if they are to count as valid.
```

Translation now:

```
- Os métodos devem produzir resultados comparáveis e reproduzíveis para serem considerados válidos.
```

- [ ] renders the repaired English — register ok
- [ ] renders the repaired English — register needs work: 
- [ ] still renders ISO's sentence — rewrite

### isms-ia-04-07-two-sentences · es-419  (1 paragraph)

**57.** block 1, line 1

English now:

```
Its normative body, in full, as ISO/IEC 27001:2022 now carries it:
```

Translation now:

```
Su cuerpo normativo, en su totalidad, tal como la recoge ahora la norma ISO/IEC 27001:2022:
```

- [ ] renders the repaired English — register ok
- [ ] renders the repaired English — register needs work: 
- [ ] still renders ISO's sentence — rewrite

### isms-ia-04-07-two-sentences · pt-BR  (1 paragraph)

**58.** block 1, line 1

English now:

```
Its normative body, in full, as ISO/IEC 27001:2022 now carries it:
```

Translation now:

```
Seu corpo normativo, na íntegra, tal como a ISO/IEC 27001:2022 o apresenta agora:
```

- [ ] renders the repaired English — register ok
- [ ] renders the repaired English — register needs work: 
- [ ] still renders ISO's sentence — rewrite

---

## Not on this list, and not a reading task

**80 further gated rows** have no survivingtranslation record. Gated rows that no surviving translation spec covers. NOT a reading task. The retranslator always renders from the CURRENT English, so anything it processed is safe by the ordering argument; what is missing is the RECORD. retranslate-spec.json is the DEFAULT --out, so every run launched without an explicit --out overwrote the previous run's record, and `lessons` has no updated_at to fall back on. Re-running the translator on these lessons resolves them mechanically and needs no human.

| cert / language | rows |
|---|---|
| AIMS-F / es-419 | 34 |
| AIMS-F / pt-BR | 34 |
| AIMS-IA / es-419 | 5 |
| AIMS-IA / pt-BR | 5 |
| ISMS-IA / es-419 | 1 |
| ISMS-IA / pt-BR | 1 |
