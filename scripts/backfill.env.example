# Secrets for scripts\backfill-practice.mjs — paste your real values, save.
# This file is gitignored. SUPABASE_URL and CERT_ID already default in the
# script (this project + the SM-I cert), so you only need these two:

SUPABASE_SERVICE_ROLE_KEY=PASTE_SERVICE_ROLE_KEY_HERE
ANTHROPIC_API_KEY=PASTE_ANTHROPIC_KEY_HERE

# Optional overrides (uncomment to use):
# FLOOR=10
# CHUNK=8
# MAX_TASKS=0
# TASK_ID=
# DRY_RUN=1
